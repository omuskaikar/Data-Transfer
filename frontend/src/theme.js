// src/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: "#d359ff",
    },
    secondary: {
      main: "#d359ff",
    },
    background: {
      default: "#1e1e2f",
      paper: "#282c34",
    },
    text: {
      primary: "#ffffff",
      secondary: "#d359ff",
    },
  },
});

export default theme;
